name: TAYVORIQ Publish Human Voice Sample (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Manual placeholder only. No Telegram message is sent."
