name: TAYVORIQ TikTok Decision Bridge (decommissioned)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  decommissioned:
    runs-on: ubuntu-latest
    steps:
      - name: Explain unified callback handling
        run: |
          echo "TikTok callbacks are now handled by tayvoriq-telegram-decision-bridge.yml."
          echo "This workflow intentionally does not call Telegram getUpdates."
