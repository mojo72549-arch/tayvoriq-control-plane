name: TAYVORIQ Narrator Voice Sample V3 (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled: no Telegram test notifications"
