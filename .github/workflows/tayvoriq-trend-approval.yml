name: TAYVORIQ Trend Approval · Legacy deaktiviert

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Legacy-getUpdates-Poller deaktiviert."
          echo "Telegram-Callbacks werden ausschließlich durch den zentralen Studio-Poller verarbeitet."
