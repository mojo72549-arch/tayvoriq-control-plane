name: TAYVORIQ Telegram Decision Bridge V2

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/tayvoriq-telegram-decision-bridge.yml
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: write
  actions: read

concurrency:
  group: tayvoriq-telegram-decision-bridge-v2
  cancel-in-progress: false

env:
  PYTHONUTF8: "1"
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
  YOUTUBE_PRIVACY: "public"

jobs:
  process-decisions:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Checkout control plane
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Process Telegram approval callbacks
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          import os
          import re
          import urllib.error
          import urllib.request
          from datetime import datetime, timezone
          from pathlib import Path

          token=os.environ.get('TELEGRAM_BOT_TOKEN','').strip()
          expected_chat=os.environ.get('TELEGRAM_CHAT_ID','').strip()
          github_token=os.environ.get('GH_TOKEN','').strip()
          repository=os.environ['GITHUB_REPOSITORY']
          privacy=os.environ.get('YOUTUBE_PRIVACY','public')
          if not token or not expected_chat or not github_token:
              raise SystemExit('Required Telegram/GitHub configuration missing')

          state_path=Path('.automation/tayvoriq-telegram-decision-state.json')
          state={'next_update_id':0,'decisions':{}}
          if state_path.is_file():
              try:
                  loaded=json.loads(state_path.read_text(encoding='utf-8'))
                  if isinstance(loaded,dict): state.update(loaded)
              except Exception:
                  pass
          state.setdefault('decisions',{})

          def telegram(method,payload):
              req=urllib.request.Request(
                  f'https://api.telegram.org/bot{token}/{method}',
                  data=json.dumps(payload,ensure_ascii=False).encode('utf-8'),
                  headers={'Content-Type':'application/json'},
                  method='POST',
              )
              with urllib.request.urlopen(req,timeout=35) as response:
                  value=json.loads(response.read().decode('utf-8'))
              if not value.get('ok'): raise RuntimeError(value)
              return value.get('result')

          def github(method,url,payload=None):
              body=None if payload is None else json.dumps(payload).encode('utf-8')
              req=urllib.request.Request(
                  url,
                  data=body,
                  headers={
                      'Authorization':f'Bearer {github_token}',
                      'Accept':'application/vnd.github+json',
                      'X-GitHub-Api-Version':'2022-11-28',
                      'Content-Type':'application/json',
                  },
                  method=method,
              )
              with urllib.request.urlopen(req,timeout=35) as response:
                  raw=response.read()
              return json.loads(raw.decode('utf-8')) if raw else {}

          def control_artifact_exists(review_id):
              try:
                  payload=github('GET',f'https://api.github.com/repos/{repository}/actions/runs/{review_id}/artifacts')
              except (urllib.error.HTTPError,urllib.error.URLError,TimeoutError):
                  return False
              expected=f'tayvoriq-control-review-{review_id}'
              return any(
                  isinstance(item,dict)
                  and item.get('name')==expected
                  and not item.get('expired')
                  for item in payload.get('artifacts',[])
              )

          updates=telegram('getUpdates',{
              'offset':int(state.get('next_update_id') or 0),
              'limit':100,
              'timeout':0,
              'allowed_updates':['callback_query'],
          }) or []
          pattern=re.compile(r'^(tayvoriq|tayvoriq-direct|tayvoriqcp):(approve|reject):(\d{6,})$')
          handled=0

          for update in updates:
              update_id=int(update.get('update_id') or 0)
              state['next_update_id']=max(int(state.get('next_update_id') or 0),update_id+1)
              callback=update.get('callback_query') if isinstance(update.get('callback_query'),dict) else {}
              callback_id=str(callback.get('id') or '').strip()
              match=pattern.fullmatch(str(callback.get('data') or '').strip())
              if not callback_id or not match: continue

              namespace,action,review_id=match.groups()
              message=callback.get('message') if isinstance(callback.get('message'),dict) else {}
              chat=message.get('chat') if isinstance(message.get('chat'),dict) else {}
              chat_id=str(chat.get('id') or '').strip()
              message_id=int(message.get('message_id') or 0)
              if chat_id != expected_chat:
                  telegram('answerCallbackQuery',{
                      'callback_query_id':callback_id,
                      'text':'Nicht autorisierter Chat.',
                      'show_alert':True,
                  })
                  continue

              is_control=(namespace=='tayvoriqcp') or control_artifact_exists(review_id)
              effective_namespace='tayvoriqcp' if is_control else namespace
              if effective_namespace=='tayvoriqcp':
                  review_path=Path(f'.automation/tayvoriq-control-reviews/{review_id}.json')
              elif effective_namespace=='tayvoriq-direct':
                  review_path=Path(f'.automation/tayvoriq-direct-reviews/{review_id}.json')
              else:
                  review_path=Path(f'.automation/tayvoriq-reviews/{review_id}.json')
              review_path.parent.mkdir(parents=True,exist_ok=True)
              try:
                  review=json.loads(review_path.read_text(encoding='utf-8')) if review_path.is_file() else {}
              except Exception:
                  review={}

              previous=str(review.get('status') or state['decisions'].get(review_id,{}).get('status') or '').upper()
              if previous in {'APPROVED','REJECTED'}:
                  telegram('answerCallbackQuery',{
                      'callback_query_id':callback_id,
                      'text':'Diese Review wurde bereits verarbeitet.',
                  })
                  if message_id:
                      telegram('editMessageReplyMarkup',{
                          'chat_id':chat_id,
                          'message_id':message_id,
                          'reply_markup':{'inline_keyboard':[]},
                      })
                  continue

              status='APPROVED' if action=='approve' else 'REJECTED'
              if is_control and status=='APPROVED':
                  github('POST',f'https://api.github.com/repos/{repository}/dispatches',{
                      'event_type':'tayvoriq_control_publish_approved',
                      'client_payload':{
                          'review_run_id':review_id,
                          'youtube_privacy':'public',
                          'approval_source':'telegram_callback',
                      },
                  })

              review.update({
                  'review_id':review_id,
                  'status':status,
                  'processed_at':datetime.now(timezone.utc).isoformat(),
                  'source':'telegram_callback',
                  'callback_namespace':namespace,
                  'effective_callback_namespace':effective_namespace,
                  'telegram_message_id':message_id,
                  'publication_pending':status=='APPROVED',
                  'youtube_privacy':'public' if is_control and status=='APPROVED' else review.get('youtube_privacy'),
                  'platform_upload_performed':False,
              })
              review_path.write_text(json.dumps(review,ensure_ascii=False,indent=2)+'\n',encoding='utf-8')
              state['decisions'][review_id]=review

              telegram('answerCallbackQuery',{
                  'callback_query_id':callback_id,
                  'text':(
                      'Freigabe erkannt. Öffentliche YouTube-Veröffentlichung startet.'
                      if is_control and status=='APPROVED'
                      else 'Freigabe gespeichert.' if status=='APPROVED'
                      else 'Review abgelehnt.'
                  ),
              })
              if message_id:
                  telegram('editMessageReplyMarkup',{
                      'chat_id':chat_id,
                      'message_id':message_id,
                      'reply_markup':{'inline_keyboard':[]},
                  })
              if status=='REJECTED':
                  text=f'❌ TAYVORIQ Review {review_id} abgelehnt. Es wird nichts veröffentlicht.'
              elif is_control:
                  text=f'✅ Freigabe erkannt.\n\nReview: {review_id}\nYouTube: Public\nNach Abschluss folgt der öffentliche YouTube-Link.'
              else:
                  text=f'✅ TAYVORIQ Review {review_id} freigegeben.'
              telegram('sendMessage',{'chat_id':expected_chat,'text':text})
              handled+=1

          state['last_checked_at']=datetime.now(timezone.utc).isoformat()
          state['last_handled_count']=handled
          state_path.parent.mkdir(parents=True,exist_ok=True)
          state_path.write_text(json.dumps(state,ensure_ascii=False,indent=2)+'\n',encoding='utf-8')
          print({'updates':len(updates),'handled':handled,'next_update_id':state.get('next_update_id')})
          PY

      - name: Persist decisions
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .automation/tayvoriq-telegram-decision-state.json \
                  .automation/tayvoriq-reviews \
                  .automation/tayvoriq-direct-reviews \
                  .automation/tayvoriq-control-reviews 2>/dev/null || true
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Record TAYVORIQ Telegram decision"
          git pull --rebase origin main
          git push origin main
