name: TAYVORIQ Telegram Decision Bridge · Legacy deaktiviert

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Zweiter getUpdates-Consumer deaktiviert."
          echo "Trend- und Review-Callbacks werden zentral im privaten Studio verarbeitet."
