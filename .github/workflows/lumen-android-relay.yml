name: Project Lumen Legacy Relay – manual only

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-relay-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: echo "The legacy duplicate Project Lumen relay is retired. Use the focused RC6 build workflow."
