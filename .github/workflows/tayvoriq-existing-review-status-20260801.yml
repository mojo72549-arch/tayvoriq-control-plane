name: TAYVORIQ Minimal Review Status 2026-08-01

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/tayvoriq-existing-review-status-20260801.yml

permissions:
  actions: read
  contents: write

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Capture minimal Telegram delivery run
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p .automation/tayvoriq-existing-review-status
          echo 'Target minimal delivery commit: 61ca91e97964437e4a3772301c13230bac93b62e'
          gh run list \
            --repo "$GITHUB_REPOSITORY" \
            --workflow tayvoriq-deliver-minimal-review-now-20260801.yml \
            --limit 5 \
            --json databaseId,createdAt,updatedAt,status,conclusion,event,headSha,url,displayTitle \
            > .automation/tayvoriq-existing-review-status/status.json
          cat .automation/tayvoriq-existing-review-status/status.json
      - name: Persist status
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .automation/tayvoriq-existing-review-status/status.json
          git commit -m "ops: record existing review delivery status"
          git pull --rebase origin main
          git push origin main
