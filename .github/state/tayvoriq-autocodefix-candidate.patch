diff --git a/runtime_patches/tayvoriq_verified_request_runtime_recovery.py b/runtime_patches/tayvoriq_verified_request_runtime_recovery.py
index d4aa1cd4..935da7a6 100644
--- a/runtime_patches/tayvoriq_verified_request_runtime_recovery.py
+++ b/runtime_patches/tayvoriq_verified_request_runtime_recovery.py
@@ -279,7 +279,8 @@ def _install_verified_request_editorial_depth(depth: Any) -> None:
         confirmed: list[str] = []
         for key in _DEPTH_KEYS:
             text = depth._clean(raw.get(key))
-            if len(text.split()) >= 5 and text not in confirmed:
+            words = text.split()
+            if len(words) >= 5 and text not in confirmed:
                 confirmed.append(text)
 
         # Fail closed. A verified request still needs three concrete source-bound
